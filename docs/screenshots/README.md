# Screenshots

Drop README images here.

- `example-scan.png` - an MCP client (Claude Desktop / Kiro / MCP Inspector) showing a
  Well-Architected scan result. Referenced from the top-level README.

Sanitize before committing: mask account IDs, ARNs, and resource IDs (security group
IDs, bucket names, etc.) since this repo is public.
